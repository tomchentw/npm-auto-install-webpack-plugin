/* eslint-disable import/no-unresolved */
import {
  default as React,
} from "react";

import {
  default as ReactDOM,
} from "react-dom";

ReactDOM.render(<div />, document.getElementById(`react-container`));
